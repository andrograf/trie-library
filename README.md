# trie-library

```
Auto-complete feature using Trie data structure.

When the user types in a prefix of his search query, 
the application will give them all recommendations
to auto-complete their query based on the strings stored in the Trie.

Using NUnit for Unit tests.
```
